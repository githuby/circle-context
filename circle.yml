machine:
  timezone:
    America/Los_Angeles # Set the timezone
  # Version of ruby to use
  ruby:
    version:
      1.8.7-p358-falcon-perf
  services:
    - cassandra
    - elasticsearch
    - rabbitmq-server
    - riak
    - beanstalkd
    - couchbase-server
    - neo4j
  test:
    override:
      - echo "hello world"
      - pwd
      - export foo="bar"
      - exit 3
    post:
      - export
      - echo $foo
